const $=id=>document.getElementById(id);const fa=n=>String(n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);
const defaults=[{id:1,name:'چای ممتاز ایرانی',cat:'چای',price:185000,old:220000,emoji:'🍵',desc:'چای خوش‌عطر و تازه برای مصرف روزانه'},{id:2,name:'چای سبز ممتاز',cat:'چای',price:245000,old:0,emoji:'🍃',desc:'طعم ملایم و عطر طبیعی برگ چای'},{id:3,name:'برنج هاشمی گیلان',cat:'برنج',price:680000,old:750000,emoji:'🌾',desc:'برنج ایرانی ممتاز با عطر و پخت عالی'},{id:4,name:'برنج طارم درجه یک',cat:'برنج',price:590000,old:0,emoji:'🌾',desc:'دانه بلند و خوش‌پخت برای سفره ایرانی'},{id:5,name:'عسل کوهستان طبیعی',cat:'عسل',price:390000,old:450000,emoji:'🍯',desc:'عسل طبیعی با طعم غنی و عطر گل‌های کوهستان'},{id:6,name:'عسل بهارنارنج',cat:'عسل',price:430000,old:0,emoji:'🍯',desc:'عطر دلنشین بهارنارنج و طعم لطیف'}];
let products=JSON.parse(localStorage.getItem('greenhome-products')||'null')||defaults,filter='همه',cart=JSON.parse(localStorage.getItem('greenhome-cart')||'[]');
const money=n=>new Intl.NumberFormat('fa-IR').format(n)+' تومان';
function save(){localStorage.setItem('greenhome-products',JSON.stringify(products));localStorage.setItem('greenhome-cart',JSON.stringify(cart))}function toast(t){$('toast').textContent=t;$('toast').classList.add('show');setTimeout(()=>$('toast').classList.remove('show'),1700)}
function render(){let q=$('search').value.trim().toLowerCase(),arr=products.filter(p=>(filter==='همه'||p.cat===filter)&&(!q||(p.name+' '+p.cat+' '+p.desc).toLowerCase().includes(q)));const s=$('sort').value;if(s==='low')arr.sort((a,b)=>a.price-b.price);if(s==='high')arr.sort((a,b)=>b.price-a.price);$('productGrid').innerHTML=arr.map(p=>`<article class="product"><div class="product-media"><span>${p.emoji||'📦'}</span>${p.old&&p.old>p.price?`<i class="discount">${fa(Math.round((1-p.price/p.old)*100))}٪ تخفیف</i>`:''}</div><div class="product-body"><small class="product-cat">${p.cat}</small><h3>${p.name}</h3><p>${p.desc||'محصول باکیفیت خانه سبز'}</p><div class="price-row"><div class="price"><b>${money(p.price)}</b>${p.old&&p.old>p.price?`<small>${money(p.old)}</small>`:''}</div><button class="add" data-add="${p.id}">افزودن به سبد</button></div></div></article>`).join('')||'<div style="grid-column:1/-1;text-align:center;padding:50px;background:#fff;border-radius:18px">محصولی پیدا نشد.</div>';$('productGrid').querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{cart.push(+b.dataset.add);save();renderCart();toast('محصول به سبد اضافه شد')});renderAdmin()}
function renderCart(){$('cartCount').textContent=fa(cart.length)}function openModal(){$('adminModal').classList.add('open');$('loginBox').style.display='block';$('panelBox').style.display='none';$('adminCode').value='';setTimeout(()=>$('adminCode').focus(),100)}
function renderAdmin(){$('adminList').innerHTML=products.map(p=>`<div class="admin-item"><span class="mini">${p.emoji||'📦'}</span><div><b>${p.name}</b><small>${p.cat} · ${money(p.price)}</small></div><button class="delete" data-del="${p.id}">حذف</button></div>`).join('');$('adminList').querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{products=products.filter(p=>p.id!==+b.dataset.del);save();render();toast('محصول حذف شد')})}
$('adminBtn').onclick=openModal;document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>$('adminModal').classList.remove('open'));$('adminModal').onclick=e=>{if(e.target===$('adminModal'))$('adminModal').classList.remove('open')};$('loginBtn').onclick=()=>{if($('adminCode').value==='1403'){$('loginBox').style.display='none';$('panelBox').style.display='block';renderAdmin()}else toast('کد مدیریت اشتباه است')};$('adminCode').onkeydown=e=>{if(e.key==='Enter')$('loginBtn').click()};$('logoutBtn').onclick=()=>{$('panelBox').style.display='none';$('loginBox').style.display='block'};
$('productForm').onsubmit=e=>{e.preventDefault();products.unshift({id:Date.now(),name:$('pName').value.trim(),cat:$('pCategory').value,price:+$('pPrice').value,old:+$('pOld').value||0,emoji:$('pEmoji').value.trim()||'📦',desc:$('pDesc').value.trim()||'محصول باکیفیت خانه سبز'});save();render();e.target.reset();toast('محصول با موفقیت اضافه شد')};document.querySelectorAll('.cat').forEach(b=>b.onclick=()=>{document.querySelectorAll('.cat').forEach(x=>x.classList.remove('active'));b.classList.add('active');filter=b.dataset.filter;render()});$('search').oninput=render;$('searchBtn').onclick=render;$('sort').onchange=render;$('cartBtn').onclick=()=>toast(cart.length?`سبد خرید: ${fa(cart.length)} محصول`:'سبد خرید خالی است');render();renderCart();