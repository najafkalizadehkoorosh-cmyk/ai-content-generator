(()=>{const $=id=>document.getElementById(id),fa=n=>String(n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);const base=[{id:1,name:'برنج هاشمی شفارود',cat:'برنج',price:980000,discount:10,icon:'🌾',desc:'محصول نمونه؛ مشخصات نهایی پس از دریافت اطلاعات رسمی شرکت تکمیل می‌شود.'},{id:2,name:'برنج طارم شفارود',cat:'برنج',price:890000,discount:8,icon:'🌾',desc:'محصول نمونه فروشگاه شفارود.'},{id:3,name:'چای ایرانی شفارود',cat:'چای',price:320000,discount:12,icon:'🍵',desc:'محصول نمونه فروشگاه شفارود.'},{id:4,name:'چای سبز شفارود',cat:'چای',price:285000,discount:5,icon:'🍃',desc:'محصول نمونه فروشگاه شفارود.'},{id:5,name:'عسل شفارود',cat:'عسل',price:540000,discount:15,icon:'🍯',desc:'محصول نمونه فروشگاه شفارود.'},{id:6,name:'عسل بهارنارنج شفارود',cat:'عسل',price:620000,discount:10,icon:'🍯',desc:'محصول نمونه فروشگاه شفارود.'}];let products=JSON.parse(localStorage.getItem('shafarood-products')||'null')||base,cart=JSON.parse(localStorage.getItem('shafarood-cart')||'[]'),cat='همه',max=Infinity;const save=()=>{localStorage.setItem('shafarood-products',JSON.stringify(products));localStorage.setItem('shafarood-cart',JSON.stringify(cart))},money=n=>Number(n).toLocaleString('fa-IR')+' تومان',final=p=>Math.round(p.price*(1-(p.discount||0)/100));function toast(t){$('toast').textContent=t;$('toast').classList.add('show');clearTimeout(window.tt);window.tt=setTimeout(()=>$('toast').classList.remove('show'),1800)}function render(){const q=($('q').value||'').trim().toLowerCase();let a=products.filter(p=>(cat==='همه'||p.cat===cat)&&final(p)<=max&&(`${p.name} ${p.cat} ${p.desc}`).toLowerCase().includes(q));const s=$('sort').value;if(s==='low')a.sort((x,y)=>final(x)-final(y));if(s==='high')a.sort((x,y)=>final(y)-final(x));if(s==='discount')a.sort((x,y)=>(y.discount||0)-(x.discount||0));$('grid').innerHTML=a.map(p=>`<article class="product"><div class="photo ${p.cat==='برنج'?'rice':p.cat==='چای'?'tea':'honey'}"><span>${p.icon}</span>${p.discount?`<i class="badge">${fa(p.discount)}٪ تخفیف</i>`:''}</div><div class="info"><small>${p.cat}</small><h3>${p.name}</h3><p>${p.desc}</p><div class="stars">★★★★★ <span>۴.۸</span></div><div class="priceRow"><div>${p.discount?`<div class="old">${money(p.price)}</div>`:''}<div class="price">${money(final(p))}</div></div><button class="add" data-add="${p.id}">افزودن به سبد</button></div></div></article>`).join('');$('empty').classList.toggle('hidden',a.length>0);document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>add(+b.dataset.add));$('count').textContent=fa(cart.reduce((n,x)=>n+x.qty,0))}function add(id){const x=cart.find(x=>x.id===id);if(x)x.qty++;else cart.push({id,qty:1});save();render();toast('محصول به سبد اضافه شد')}function cartRender(){let total=0;$('items').innerHTML=cart.map(x=>{const p=products.find(y=>y.id===x.id);if(!p)return '';total+=final(p)*x.qty;return `<div class="cartItem"><div class="cartIcon">${p.icon}</div><div><h4>${p.name}</h4><div class="qty"><button data-q="${p.id}|-1">−</button><span>${fa(x.qty)}</span><button data-q="${p.id}|1">+</button></div></div><b>${money(final(p)*x.qty)}</b></div>`}).join('')||'<p class="muted">سبد خرید خالی است.</p>';$('total').textContent=money(total);document.querySelectorAll('[data-q]').forEach(b=>b.onclick=()=>{const [id,d]=b.dataset.q.split('|').map(Number),x=cart.find(y=>y.id===id);if(x){x.qty+=d;if(x.qty<1)cart=cart.filter(y=>y.id!==id);save();cartRender();render()}})}function open(id){$(id).classList.add('show');if(id==='cartModal')cartRender()}function close(id){$(id).classList.remove('show')}document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>close(b.dataset.close));document.querySelectorAll('.modal').forEach(m=>m.onclick=e=>{if(e.target===m)close(m.id)});$('q').oninput=render;$('searchBtn').onclick=render;$('sort').onchange=render;$('cartBtn').onclick=()=>open('cartModal');document.querySelectorAll('[data-cat]').forEach(b=>b.onclick=e=>{e.preventDefault();cat=b.dataset.cat;max=Infinity;document.querySelectorAll('.filter').forEach(x=>x.classList.toggle('active',x.dataset.cat===cat));render();$('products').scrollIntoView({behavior:'smooth'})});document.querySelectorAll('[data-max]').forEach(b=>b.onclick=()=>{max=+b.dataset.max;cat='همه';render()});$('checkout').onclick=()=>{if(!cart.length)return toast('سبد خرید خالی است');close('cartModal');open('checkoutModal')};$('checkoutForm').onsubmit=e=>{e.preventDefault();cart=[];save();close('checkoutModal');render();toast('درخواست سفارش ثبت شد')};let ok=false;$('adminBtn').onclick=()=>open('adminModal');$('loginBtn').onclick=()=>{if($('code').value==='1403'){ok=true;$('login').classList.add('hidden');$('panel').classList.remove('hidden');adminRender()}else toast('کد مدیریت اشتباه است')};$('logout').onclick=()=>{ok=false;$('panel').classList.add('hidden');$('login').classList.remove('hidden');$('code').value=''};$('productForm').onsubmit=e=>{e.preventDefault();if(!ok)return;products.push({id:Date.now(),name:$('name').value.trim(),cat:$('cat').value,price:+$('price').value,discount:+$('disc').value||0,icon:$('icon').value||'📦',desc:$('desc').value.trim()||'محصول شفارود'});save();render();adminRender();e.target.reset();toast('محصول اضافه شد')};function adminRender(){$('adminList').innerHTML=products.map(p=>`<div class="adminRow"><div class="ico">${p.icon}</div><div><b>${p.name}</b><small>${p.cat} · ${money(final(p))}</small></div><button data-del="${p.id}">حذف</button></div>`).join('');document.querySelectorAll('[data-del]').forEach(b=>b.onclick=()=>{products=products.filter(p=>p.id!==+b.dataset.del);save();render();adminRender();toast('محصول حذف شد')})}render()})();